{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ice Cherry",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "args": ["-config", "${workspaceFolder}/test/data/ice-cherry.config.json"]
        }
    ]
}
