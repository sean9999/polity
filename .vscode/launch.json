{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "debug polity",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/polity/main.go",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
